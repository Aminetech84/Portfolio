

/*

function active() {
    let elem = document.querySelector('button');
    console.log(elem.classList);

    if (elem.classList.contains('1') === true) {
        elem.addEventListener("click", () => {
            elem.classList.add('active');
            console.log(elem.classList);
            
    });
        console.log(elem.classList);
        elem.addEventListener("click", () => elem.classList.remove('active'));
        console.log(elem.classList);
    } else {
        console.log(':(');
    }
    console.log(elem);
}

active();


console.log(elem);
/*

function active() {
    document.querySelector('button').classList.add("active");
}

document.querySelector('button').classList.add("active");




if (elem.classList.contains('1') === true) {
        document.addEventListener("click", () => document.getElementsByClassName('1').classList.add('active'));
        console.log(elem);
        
    } else {
        console.log(':(');
    }
    console.log(elem);
}
*/